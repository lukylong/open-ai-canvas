---
title: 待生产确认
description: 已实现但需要生产数据或外部服务确认的项目
---

# 待生产确认

- 画布网站 Agent 的工具批准已改为一次性领取执行上下文：首次点击立即进入“执行中”并禁用批准/拒绝，同一批准卡的后续点击不会再次创建节点或提交生成任务；待在已登录真实画布用包含“新增节点 + 图片生成”的批准卡连续点击确认一次执行、一次任务。
- 画布网站 Agent 的“怎么设置画风”帮助意图已改为固定调用只读画风指引工具：返回项目画布左侧齿轮 → 项目设置 → 选择/更换画风 → 保存，以及独立画布底部“+”→“项目画风”两条真实入口；不会再误启动影视 Agent 后报“请先设置项目画风”。待在已登录浏览器分别用未设置/已设置画风的项目画布确认文案与入口。
- 迁移账号使用旧 bcrypt 密码登录、修改密码后所有会话失效。
- 删除迁移素材时确认 `retain_shared` 只清理影策引用，不删除 ZQ 共用 COS 对象。
- 素材分发平台的迁移素材、私有 COS 新素材和重复 event 已完成生产发布；仍需从浏览器覆盖当前选择、完整系列、1000 条、跨 1000 分片、部分失败和 409 冲突提示。
- 15 个 ComfyUI workflow 在实际 GPU 节点覆盖、输出类型、取消和重启续查；其中 MiniMax H3 v0.33.1 统一文生视频/图生视频工作流已完成单次真实音画输出，仍需覆盖长时长、9 张参考图、取消和重启续查。
- ZQ 声音样本通过实际异步 TTS Provider 生成音频。
- 专业导演两方案在浏览器完成选择后才创建任务，并正确写回画布。
- 管理后台“用户内容”在生产对象存储上完成跨用户图片、视频、音频预览和 Range 下载，并确认普通用户不能调用管理接口。
- 持久化图片批次在真实平台模型上验证 1、15、16 和 1000 数量；确认浏览器关闭、后端重启、暂停恢复、取消等待项和失败重试后任务数量、积分与素材数量一致。
- 网站 Agent 在真实登录画布中验证：旧文本 SKU 自动纠正到逻辑文本模型、`canvas_text` 任务带项目归属、失败信息不会被项目会话回写覆盖，并覆盖刷新后的对话恢复。
